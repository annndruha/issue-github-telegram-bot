query getContinentName ($ghquery: String!, $cursor: String!) {
    repos: search(
        query: $ghquery
        type: REPOSITORY
        last: 9
        before: $cursor
    ) {
        repositoryCount
        pageInfo {
            hasNextPage
            endCursor
            hasPreviousPage
            startCursor
        }
        edges {
            node {
                ... on Repository {
                    name
                    url
                }
            }
        }
    }
}
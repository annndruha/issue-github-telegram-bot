schema: schema.github.graphql
extensions:
  endpoints:
    Default GraphQL Endpoint:
      url: https://api.github.com/graphql
      headers:
        Accept: application/vnd.github+json
        Authorization: Bearer ${GH_ACCOUNT_TOKEN}
      introspect: false
